# PubQuizStats Configuration

# Database URL (SQLite)
DATABASE_URL=sqlite:///./data/pubquizstats.db

# Fuzzy matching threshold (0-1)
FUZZY_MATCH_THRESHOLD=0.85

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Excel parsing
EXCEL_SAMPLE_SIZE=5
